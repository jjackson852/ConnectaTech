<?xml version="1.0" encoding="utf-8"?>


<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bootstrap="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/refresh"
        android:icon="@drawable/ic_action_refresh"
        android:title="@string/refresh_action"
        bootstrap:showAsAction="always"></item>

    <!--<item
        android:id="@+id/timer"
        android:icon="@drawable/ic_action_timer"
        bootstrap:showAsAction="always"
        android:title="@string/title_timer">
    </item>

    <item
        android:id="@+id/rating"
        bootstrap:showAsAction="always"
        android:title="@string/rating_title">
    </item>

    <item
        android:id="@+id/test"
        bootstrap:showAsAction="always"
        android:title="@string/title_test">
    </item>-->


    <item
        android:id="@+id/logout"
        android:title="@string/logout_action"
        bootstrap:showAsAction="never"></item>


</menu>